Prerequisites (latest available versions):
    Interpreter: Python 3.x
    Packages (will be automatically installed if not present):
        - pandas
        - sklearn

Execute the python file in the extracted folder:
    For example, to execute with the system interpreter:
        0. (**For unix systems only**) alias python=python3
        1. python "Titanic - Oliver Charles Scott (7931937).py"
        
    Otherwise, to execute in a virtual environment (e.g. pipenv):
        1. pipenv run python "Titanic - Oliver Charles Scott (7931937).py"
